name: flags_app
description: Flutter Task Flags App for team Nearcast

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.11
  flutter_bloc: ^7.3.0
  equatable: ^2.0.3
  http: ^0.13.4
  sqflite: ^2.0.0+3
  cached_network_image: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/
